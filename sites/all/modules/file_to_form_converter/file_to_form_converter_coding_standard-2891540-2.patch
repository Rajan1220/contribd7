diff --git a/file_to_form_converter.info b/file_to_form_converter.info
index 5ef6ea8..8450691 100644
--- a/file_to_form_converter.info
+++ b/file_to_form_converter.info
@@ -1,4 +1,3 @@
 name = File To Form Converter
 description = This module will convert uploaded file to Form !
 core = 7.x
-version = "7.x-1.0"
\ No newline at end of file
diff --git a/file_to_form_converter.module b/file_to_form_converter.module
index 4bfeba7..27675e2 100644
--- a/file_to_form_converter.module
+++ b/file_to_form_converter.module
@@ -1,4 +1,10 @@
-<?php 
+<?php
+
+/**
+ * @file
+ * {@inheritdoc}
+ */
+
 /**
  * Implements hook_block_info().
  */
@@ -6,7 +12,7 @@ function file_to_form_converter_block_info() {
   $blocks = array();
   $blocks['file_to_form_converter'] = array(
     'info' => t('File To Form Converter'),
-    //'cache' => DRUPAL_NO_CACHE,
+    // 'cache' => DRUPAL_NO_CACHE,.
   );
   return $blocks;
 }
@@ -36,15 +42,18 @@ function file_to_form_converter_form($form, &$form_state) {
     '#descripion' => t("Only csv format File is allowed."),
     '#upload_location'    => "public://",
     "#upload_validators"  => array("file_validate_extensions" => array("csv")),
-    '#required'=>TRUE,
+    '#required' => TRUE,
   );
   $form['submit'] = array(
     '#type' => 'submit',
     '#value' => 'Converet',
-    );
+  );
   return $form;
 }
 
+/**
+ * {@inheritdoc}
+ */
 function file_to_form_converter_form_validate($form, &$form_state) {
   $validator = array(
     'file_validate_is_image',
@@ -53,10 +62,13 @@ function file_to_form_converter_form_validate($form, &$form_state) {
   // check if folder exists
   /*$dest = 'public://';
   if(!file_prepare_directory($dest)) {
-    drupal_mkdir($dest);
+  drupal_mkdir($dest);
   }*/
 }
 
+/**
+ * {@inheritdoc}
+ */
 function file_to_form_converter_form_submit($form, &$form_state) {
   drupal_set_message(t('File uploaded sucessfully !'));
 }
@@ -65,11 +77,10 @@ function file_to_form_converter_form_submit($form, &$form_state) {
  * Implements hook_preprocess_block().
  */
 /*function file_to_form_converter_preprocess_block(&$variables) {
-  if ($variables['block']->bid === 'target_block_id') {
-  
-  } 
-  else if ($variables['block']->bid === 'other_target_block_id') {
-  
-  }
+if ($variables['block']->bid === 'target_block_id') {
+
+}
+else if ($variables['block']->bid === 'other_target_block_id') {
+
+}
 }*/
-?>
\ No newline at end of file
